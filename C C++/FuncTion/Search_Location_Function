struct array
{
    int n;
    int *a;
};

void searchlocation(array p)
{
    int x;
    cout<<"nhap gia tri can tim : ";
    cin>>x;
    for (int i = 0 ; i < p.n ; i++)
    {
        if (p.a[i] == x)
        {
            cout<<i<<" ";
        }
    } 
}